---
phase: 999.3-firmware-update-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/FirmwareVersion.kt
  - AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/TeFirmwareCatalog.kt
  - AndroidApp/app/src/test/java/com/ep133/sampletool/FirmwareVersionTest.kt
  - AndroidApp/app/src/test/java/com/ep133/sampletool/TeFirmwareCatalogParserTest.kt
autonomous: true
requirements:
  - FW-02

must_haves:
  truths:
    - "FirmwareVersion parses version strings into an ordered comparable value type"
    - "parse(null) and parse(blank) return null"
    - "FirmwareVersion comparison zero-pads shorter operands so 2.5 > 2.0.5"
    - "TeFirmwareCatalog.parseVersions excludes tokens whose major component > 50 so release dates like 2026.06.24 are rejected"
    - "TeFirmwareCatalog.parseVersions returns the max valid token from a fixture HTML snippet"
    - "LATEST_KNOWN_FIRMWARE constant is 2.5"
    - "All assertions proven by passing unit tests (no Android framework needed)"
  artifacts:
    - path: "AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/FirmwareVersion.kt"
      provides: "Pure value type + parse factory"
      contains: "data class FirmwareVersion"
    - path: "AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/TeFirmwareCatalog.kt"
      provides: "FirmwareCatalog interface + TE implementation + parseVersions pure fn + LATEST_KNOWN_FIRMWARE constant"
      contains: "interface FirmwareCatalog"
    - path: "AndroidApp/app/src/test/java/com/ep133/sampletool/FirmwareVersionTest.kt"
      provides: "FirmwareVersion unit tests"
    - path: "AndroidApp/app/src/test/java/com/ep133/sampletool/TeFirmwareCatalogParserTest.kt"
      provides: "Parser + date-exclusion unit tests with fixture HTML"
  key_links:
    - from: "TeFirmwareCatalog.latestVersion()"
      to: "FirmwareVersion.parse()"
      via: "applies parse() to each extracted token; returns max remaining after date-filter"
    - from: "DeviceViewModel (Wave 2)"
      to: "FirmwareCatalog interface"
      via: "constructor-injected catalog: FirmwareCatalog = TeFirmwareCatalog()"
---

<objective>
Create the pure domain core for firmware version detection: the FirmwareVersion value type and the TeFirmwareCatalog scraper/parser. Both are network-free pure Kotlin and are fully covered by unit tests before any wiring.

Purpose: Wave 1 depends on FirmwareVersion and FirmwareCatalog being stable contracts. Getting the tests green here proves the core logic before the ViewModel and UI touch it.
Output: FirmwareVersion.kt, TeFirmwareCatalog.kt (containing the FirmwareCatalog interface), FirmwareVersionTest.kt, TeFirmwareCatalogParserTest.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md
@AndroidApp/app/src/test/java/com/ep133/sampletool/SampleImportTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: FirmwareVersion pure value type + unit tests</name>
  <files>
    AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/FirmwareVersion.kt
    AndroidApp/app/src/test/java/com/ep133/sampletool/FirmwareVersionTest.kt
  </files>
  <read_first>
    - .planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md (FirmwareVersion spec: data class, Comparable, parse, zero-padding rule)
    - AndroidApp/app/src/test/java/com/ep133/sampletool/SampleImportTest.kt (test file style: package, imports, @Test, Assert.*)
  </read_first>
  <action>
    Create the package directory AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/.

    Create FirmwareVersion.kt in that package:
    - data class FirmwareVersion(val parts: List[Int]) : Comparable[FirmwareVersion]
    - compareTo pads the shorter list with zeros to match the longer length, then compares element-by-element (lexicographic over ints)
    - companion object fun parse(raw: String?): FirmwareVersion? — returns null on null or blank input; splits on "." and maps to Int; returns null if any part is non-numeric (use toIntOrNull())
    - No Android imports. Pure Kotlin only.

    Create FirmwareVersionTest.kt in package com.ep133.sampletool:
    - parse("2.5") returns FirmwareVersion([2, 5])
    - parse("2.0.5") returns FirmwareVersion([2, 0, 5])
    - parse(null) returns null
    - parse("") returns null
    - parse("  ") returns null
    - parse("abc") returns null
    - FirmwareVersion([2,5]) > FirmwareVersion([2,0,5]) — the zero-padding rule: [2,5,0] vs [2,0,5], first diff at index 1: 5 > 0
    - FirmwareVersion([2,0,5]) > FirmwareVersion([2,0,2])
    - FirmwareVersion([2,5]) == FirmwareVersion([2,5,0]) — same after padding
    - Known TE history order (descending): 2.5 > 2.0.5 > 2.0.2 > 2.0.1 > 2.0 > 1.2.3

    Test file imports: org.junit.Test, org.junit.Assert.* — match SampleImportTest.kt style exactly (no Mockito, no coroutines, plain JUnit4).
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/ep_133_sample_tool/.claude/worktrees/elated-torvalds-b407d5/AndroidApp && ./gradlew :app:testDebugUnitTest --tests "com.ep133.sampletool.FirmwareVersionTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - FirmwareVersion.kt exists at domain/firmware/FirmwareVersion.kt
    - File contains "data class FirmwareVersion" and ": Comparable<FirmwareVersion>"
    - FirmwareVersionTest.kt exists with at least 9 @Test methods
    - gradle test command exits 0 with "BUILD SUCCESSFUL" or "1 test suite passed"
    - parse("2.5") > parse("2.0.5") is asserted and passes (zero-padding rule)
  </acceptance_criteria>
  <done>FirmwareVersion parses and orders version strings correctly; all edge-case null/blank/non-numeric inputs return null; zero-padding comparison proven by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: FirmwareCatalog interface + TeFirmwareCatalog parser + unit tests</name>
  <files>
    AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/TeFirmwareCatalog.kt
    AndroidApp/app/src/test/java/com/ep133/sampletool/TeFirmwareCatalogParserTest.kt
  </files>
  <read_first>
    - .planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md (FirmwareCatalog interface, TeFirmwareCatalog, scrape URL, date-exclusion rule, LATEST_KNOWN_FIRMWARE=2.5, timeout values, Log.w pattern, FirmwareVersion.parse usage)
    - AndroidApp/app/src/main/java/com/ep133/sampletool/domain/firmware/FirmwareVersion.kt (just created — parse() signature)
    - AndroidApp/app/src/test/java/com/ep133/sampletool/SampleImportTest.kt (test style)
  </read_first>
  <action>
    Create TeFirmwareCatalog.kt in package com.ep133.sampletool.domain.firmware:

    Define the FirmwareCatalog interface in the same file (or a separate FirmwareCatalog.kt in the same package — either is fine):
      interface FirmwareCatalog { suspend fun latestVersion(): FirmwareVersion? }

    Define constant: val LATEST_KNOWN_FIRMWARE = FirmwareVersion.parse("2.5")!!

    Define internal/package-private pure function parseVersions(html: String): FirmwareVersion? — this is the testable heart:
      1. Extract all tokens matching regex \d+\.\d+(?:\.\d+)? from the html string
      2. Parse each with FirmwareVersion.parse()
      3. Discard any where parts[0] > 50 (date-exclusion: 2026.06.24 has major=2026)
      4. Return the max of remaining tokens, or null if none

    Define class TeFirmwareCatalog : FirmwareCatalog:
      - override suspend fun latestVersion(): FirmwareVersion?
      - On Dispatchers.IO: open HttpURLConnection to https://teenage.engineering/downloads/ep-133
      - connectTimeout = 5000, readTimeout = 5000
      - On non-200 response: Log.w("EP133APP", "FW check: HTTP ${response}") and return null
      - Read body as UTF-8 string; call parseVersions(body)
      - On any exception (IOException, etc.): Log.w("EP133APP", "FW check failed: $e") and return null
      - Apply floor: return max(scraped ?: LATEST_KNOWN_FIRMWARE, LATEST_KNOWN_FIRMWARE) — never return a version below the bundled fallback

    Create TeFirmwareCatalogParserTest.kt in package com.ep133.sampletool:
    Use inline HTML fixture strings (no file I/O) that mimic realistic TE page fragments. The fixture MUST contain:
      - The token "2026.06.24" (a real date on the TE page — must be excluded)
      - Multiple valid version tokens: "2.5", "2.0.5", "2.0.2", "1.2.3"

    Test cases for parseVersions():
      - Fixture with date + valid versions: returns FirmwareVersion.parse("2.5")
      - Fixture with only the date token "2026.06.24": returns null (no valid tokens after exclusion)
      - Fixture with only "1.2.3": returns FirmwareVersion.parse("1.2.3")
      - Empty string: returns null
      - Fixture with "51.0" (major > 50) only: returns null
      - Fixture with "50.0" (major == 50, borderline): returns FirmwareVersion([50,0]) — not excluded

    Also test the floor logic:
      - max(FirmwareVersion.parse("2.3"), LATEST_KNOWN_FIRMWARE) == LATEST_KNOWN_FIRMWARE (scraped older than bundled → use bundled)
      - max(FirmwareVersion.parse("2.6"), LATEST_KNOWN_FIRMWARE) == FirmwareVersion.parse("2.6") (scraped newer → use scraped)

    No Android imports in the test. Call parseVersions() directly (make it internal or package-private accessible from test package, or expose it as a companion fun on TeFirmwareCatalog).
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/ep_133_sample_tool/.claude/worktrees/elated-torvalds-b407d5/AndroidApp && ./gradlew :app:testDebugUnitTest --tests "com.ep133.sampletool.TeFirmwareCatalogParserTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TeFirmwareCatalog.kt exists at domain/firmware/TeFirmwareCatalog.kt
    - File contains "interface FirmwareCatalog" and "LATEST_KNOWN_FIRMWARE"
    - File contains "suspend fun latestVersion()" with HttpURLConnection usage (import java.net.HttpURLConnection)
    - File contains "Log.w" call in both the non-200 branch and the catch block
    - parseVersions is callable from the test (companion fun or internal)
    - TeFirmwareCatalogParserTest.kt has a test asserting parse on a fixture containing "2026.06.24" returns 2.5 (date excluded)
    - TeFirmwareCatalogParserTest.kt has a test asserting a date-only fixture returns null
    - gradle test command exits 0
  </acceptance_criteria>
  <done>FirmwareCatalog interface and TeFirmwareCatalog are defined; parseVersions correctly excludes date tokens and returns the max valid version; bundled fallback constant is 2.5; all parser tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → teenage.engineering | First outbound network call in the app; GET only; HTTPS enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-01 | Information Disclosure | TeFirmwareCatalog HTTP response | mitigate | HTTPS only (usesCleartextTraffic=false already set in manifest); no user data sent in the GET |
| T-999.3-02 | Tampering | Scraped version token used as latest | mitigate | parseVersions validates token against \d+\.\d+(?:\.\d+)? regex and applies major>50 exclusion; FirmwareVersion.parse rejects non-numeric; floor ensures value never drops below LATEST_KNOWN_FIRMWARE |
| T-999.3-03 | Denial of Service | Network timeout blocks Device screen | mitigate | connectTimeout=5000 + readTimeout=5000 on Dispatchers.IO; exception caught and logged; screen continues showing existing stats |
| T-999.3-SC | Tampering | No new package installs this wave | accept | No npm/pip/cargo installs; pure Kotlin using java.net.HttpURLConnection from JDK |
</threat_model>

<verification>
Run all firmware-domain tests: cd AndroidApp && ./gradlew :app:testDebugUnitTest --tests "com.ep133.sampletool.FirmwareVersionTest" --tests "com.ep133.sampletool.TeFirmwareCatalogParserTest"

Both test classes must pass. BUILD SUCCESSFUL.
</verification>

<success_criteria>
- FirmwareVersion.parse("2.5") > FirmwareVersion.parse("2.0.5") is asserted and green
- parseVersions on a fixture containing "2026.06.24" and "2.5" returns parse("2.5")
- parseVersions on a date-only fixture returns null
- LATEST_KNOWN_FIRMWARE == FirmwareVersion.parse("2.5")
- No Android framework imports in either source file
</success_criteria>

<output>
Create `.planning/phases/999.3-firmware-update-detection/999.3-firmware-update-detection-01-SUMMARY.md` when done.
</output>
