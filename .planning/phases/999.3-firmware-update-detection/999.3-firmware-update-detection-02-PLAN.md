---
phase: 999.3-firmware-update-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 999.3-firmware-update-detection-01
files_modified:
  - AndroidApp/app/src/main/AndroidManifest.xml
  - AndroidApp/app/build.gradle.kts
  - AndroidApp/app/src/main/java/com/ep133/sampletool/ui/device/DeviceScreen.kt
  - AndroidApp/app/src/test/java/com/ep133/sampletool/DeviceViewModelFirmwareTest.kt
autonomous: true
requirements:
  - FW-01
  - FW-02

must_haves:
  truths:
    - "DeviceViewModel exposes firmwareUpdate: StateFlow<FirmwareUpdateState> with five subtypes"
    - "checkFirmwareUpdate() fires after loadStats() succeeds and deviceState.firmwareVersion is non-null"
    - "Unparseable firmware string produces FirmwareUpdateState.Unknown, not a crash"
    - "Network failure during catalog.latestVersion() produces FirmwareUpdateState.Unknown"
    - "UpdateAvailable holds both current and latest FirmwareVersion"
    - "INTERNET permission and androidx.browser dependency are declared"
    - "DeviceViewModelFirmwareTest passes with a fake FirmwareCatalog — no network"
  artifacts:
    - path: "AndroidApp/app/src/main/AndroidManifest.xml"
      provides: "INTERNET permission"
      contains: "android.permission.INTERNET"
    - path: "AndroidApp/app/build.gradle.kts"
      provides: "androidx.browser dependency"
      contains: "androidx.browser:browser"
    - path: "AndroidApp/app/src/main/java/com/ep133/sampletool/ui/device/DeviceScreen.kt"
      provides: "FirmwareUpdateState sealed class + DeviceViewModel firmware state/check logic"
      contains: "FirmwareUpdateState"
    - path: "AndroidApp/app/src/test/java/com/ep133/sampletool/DeviceViewModelFirmwareTest.kt"
      provides: "ViewModel firmware state tests with fake catalog"
  key_links:
    - from: "DeviceViewModel.loadStats()"
      to: "DeviceViewModel.checkFirmwareUpdate()"
      via: "called in finally block after midi.queryDeviceStats() in the same loadStats() coroutine"
    - from: "DeviceViewModel.checkFirmwareUpdate()"
      to: "catalog.latestVersion()"
      via: "viewModelScope.launch + Dispatchers.IO-wrapped catalog call"
    - from: "FirmwareUpdateState.UpdateAvailable"
      to: "DeviceScreen banner (Wave 3)"
      via: "firmwareUpdate StateFlow collected in DeviceScreen composable"
---

<objective>
Wire FirmwareUpdateState into DeviceViewModel and add the INTERNET permission and androidx.browser dependency. A fake FirmwareCatalog in the test suite proves the state machine is correct without any network calls. This plan also declares the open-updater callback stub (onOpenFirmwareUpdater / openFirmwareUpdater) ready for MainActivity wiring in Wave 3.

Purpose: The ViewModel state machine is the core behavior of FW-01/FW-02. Getting it tested in isolation before any UI is added keeps the Unit test loop fast and the UI plan simple.
Output: FirmwareUpdateState sealed type + DeviceViewModel firmware fields/logic (in DeviceScreen.kt alongside DeviceViewModel), manifest INTERNET permission, build.gradle.kts browser dependency, DeviceViewModelFirmwareTest.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md
@.planning/phases/999.3-firmware-update-detection/999.3-firmware-update-detection-01-SUMMARY.md
@AndroidApp/app/src/main/java/com/ep133/sampletool/ui/device/DeviceScreen.kt
@AndroidApp/app/src/main/AndroidManifest.xml
@AndroidApp/app/build.gradle.kts
@AndroidApp/app/src/test/java/com/ep133/sampletool/SampleImportTest.kt

<interfaces>
From domain/firmware/FirmwareVersion.kt (created in Plan 01):
  data class FirmwareVersion(val parts: List<Int>) : Comparable<FirmwareVersion>
  companion object { fun parse(raw: String?): FirmwareVersion? }

From domain/firmware/TeFirmwareCatalog.kt (created in Plan 01):
  interface FirmwareCatalog { suspend fun latestVersion(): FirmwareVersion? }
  val LATEST_KNOWN_FIRMWARE: FirmwareVersion
  class TeFirmwareCatalog : FirmwareCatalog

From ui/device/DeviceScreen.kt (existing DeviceViewModel — key fields):
  class DeviceViewModel(private val midi: MIDIRepository) : ViewModel()
  val deviceState: StateFlow<DeviceState> = midi.deviceState
  private val _statsLoading = MutableStateFlow(false)
  val statsLoading: StateFlow<Boolean>
  var onRequestBackup: ((suggestedName: String) -> Unit)? = null
  var onRequestRestore: (() -> Unit)? = null
  fun loadStats() — calls midi.queryDeviceStats() in viewModelScope; _statsLoading guards it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add INTERNET permission and androidx.browser dependency</name>
  <files>
    AndroidApp/app/src/main/AndroidManifest.xml
    AndroidApp/app/build.gradle.kts
  </files>
  <read_first>
    - AndroidApp/app/src/main/AndroidManifest.xml (current state — insert permission before application tag)
    - AndroidApp/app/build.gradle.kts (current dependencies block — add browser after webkit line)
    - .planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md (usesCleartextTraffic stays false; browser dep for CustomTabsIntent)
  </read_first>
  <action>
    In AndroidManifest.xml, add before the opening application tag:
      &lt;uses-permission android:name="android.permission.INTERNET" /&gt;
    Do NOT add android:usesCleartextTraffic="true" — it is already "false" and must stay that way.

    In build.gradle.kts, add to the dependencies block (after the webkit line is a natural location):
      implementation("androidx.browser:browser:1.8.0")

    No other changes. Two files only.
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/ep_133_sample_tool/.claude/worktrees/elated-torvalds-b407d5/AndroidApp && grep -c "android.permission.INTERNET" app/src/main/AndroidManifest.xml && grep -c "androidx.browser:browser" app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains exactly one line matching "android.permission.INTERNET"
    - AndroidManifest.xml still contains "android:usesCleartextTraffic=\"false\"" (unchanged)
    - build.gradle.kts contains "androidx.browser:browser"
    - Both grep commands above return 1
  </acceptance_criteria>
  <done>INTERNET permission declared; androidx.browser on the classpath; cleartext traffic stays disabled.</done>
</task>

<task type="auto">
  <name>Task 2: FirmwareUpdateState + DeviceViewModel firmware logic + ViewModel test</name>
  <files>
    AndroidApp/app/src/main/java/com/ep133/sampletool/ui/device/DeviceScreen.kt
    AndroidApp/app/src/test/java/com/ep133/sampletool/DeviceViewModelFirmwareTest.kt
  </files>
  <read_first>
    - AndroidApp/app/src/main/java/com/ep133/sampletool/ui/device/DeviceScreen.kt (read entirely — understand existing DeviceViewModel fields, loadStats() body, and import list before adding anything)
    - .planning/phases/999.3-firmware-update-detection/999.3-CONTEXT.md (FirmwareUpdateState variants, catalog injection pattern, onOpenFirmwareUpdater/openFirmwareUpdater, Unknown on unparseable firmware, Log.w on catalog failure)
    - AndroidApp/app/src/test/java/com/ep133/sampletool/SampleImportTest.kt (test double style)
    - .planning/phases/999.3-firmware-update-detection/999.3-firmware-update-detection-01-SUMMARY.md (FirmwareVersion and FirmwareCatalog interfaces from Plan 01)
  </read_first>
  <action>
    In DeviceScreen.kt, at the top of the file (after existing imports, before class DeviceViewModel):

    Add sealed class FirmwareUpdateState:
      - object Idle : FirmwareUpdateState()
      - object Checking : FirmwareUpdateState()
      - object UpToDate : FirmwareUpdateState()
      - data class UpdateAvailable(val current: FirmwareVersion, val latest: FirmwareVersion) : FirmwareUpdateState()
      - object Unknown : FirmwareUpdateState()

    In DeviceViewModel class, add:
      - Constructor parameter: catalog: FirmwareCatalog = TeFirmwareCatalog()
        (per D-locked pattern: default to real impl so existing DeviceViewModel(midiRepo) call in MainActivity needs no change)
      - private val _firmwareUpdate = MutableStateFlow<FirmwareUpdateState>(FirmwareUpdateState.Idle)
        val firmwareUpdate: StateFlow<FirmwareUpdateState> = _firmwareUpdate.asStateFlow()
      - var onOpenFirmwareUpdater: (() -> Unit)? = null (mirrors onRequestBackup pattern)
      - fun openFirmwareUpdater() { onOpenFirmwareUpdater?.invoke() }
      - private fun checkFirmwareUpdate() — launches a new viewModelScope coroutine:
          _firmwareUpdate.value = FirmwareUpdateState.Checking
          val rawFw = deviceState.value.firmwareVersion
          val current = FirmwareVersion.parse(rawFw)
          if (current == null) { Log.w("EP133APP", "FW check: unparseable firmware '$rawFw'"); _firmwareUpdate.value = FirmwareUpdateState.Unknown; return@launch }
          val latest = try { catalog.latestVersion() } catch (e: Exception) { Log.w("EP133APP", "FW check failed: $e"); null }
          if (latest == null) { _firmwareUpdate.value = FirmwareUpdateState.Unknown; return@launch }
          _firmwareUpdate.value = if (current >= latest) FirmwareUpdateState.UpToDate else FirmwareUpdateState.UpdateAvailable(current, latest)

    Modify loadStats() — after the try/finally block completes (after _statsLoading.value = false in the finally), call checkFirmwareUpdate() only if deviceState.value.firmwareVersion != null. Keep the existing guard if (_statsLoading.value) return.

    Add required imports to DeviceScreen.kt:
      - import android.util.Log
      - import com.ep133.sampletool.domain.firmware.FirmwareCatalog
      - import com.ep133.sampletool.domain.firmware.FirmwareUpdateState (not needed if in same file)
      - import com.ep133.sampletool.domain.firmware.FirmwareVersion
      - import com.ep133.sampletool.domain.firmware.TeFirmwareCatalog

    Create DeviceViewModelFirmwareTest.kt in package com.ep133.sampletool:

    Test double: class FakeFirmwareCatalog(private val result: FirmwareVersion?) : FirmwareCatalog {
      var callCount = 0
      override suspend fun latestVersion(): FirmwareVersion? { callCount++; return result }
    }

    Use TestCoroutineDispatcher / runTest (kotlinx.coroutines.test) — match the pattern from existing test files.

    Create a minimal fake MIDIRepository or a subclass that returns a known DeviceState. Mirror the SampleImportFakeMIDIRepo pattern: subclass MIDIRepository with a spy port, override _deviceState.value to set a connected state with firmwareVersion.

    Test cases:
      1. firmwareVersion = "2.0.5", catalog returns parse("2.5") → final state is UpdateAvailable(current=parse("2.0.5")!!, latest=parse("2.5")!!)
      2. firmwareVersion = "2.5", catalog returns parse("2.5") → final state is UpToDate
      3. firmwareVersion = null, catalog not called → final state is Unknown; catalog.callCount == 0
      4. firmwareVersion = "not-a-version", catalog not called → final state is Unknown; catalog.callCount == 0
      5. firmwareVersion = "2.0", catalog returns null (network failure) → final state is Unknown
      6. After loadStats() is called with firmwareVersion populated, checkFirmwareUpdate is triggered → catalog.callCount == 1

    Use runTest { } from kotlinx-coroutines-test (already on testImplementation classpath per build.gradle.kts). Advance coroutines as needed with advanceUntilIdle().
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/ep_133_sample_tool/.claude/worktrees/elated-torvalds-b407d5/AndroidApp && ./gradlew :app:testDebugUnitTest --tests "com.ep133.sampletool.DeviceViewModelFirmwareTest" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - DeviceScreen.kt contains "sealed class FirmwareUpdateState"
    - DeviceScreen.kt contains "val firmwareUpdate: StateFlow<FirmwareUpdateState>"
    - DeviceScreen.kt contains "var onOpenFirmwareUpdater: (() -> Unit)? = null"
    - DeviceScreen.kt contains "fun openFirmwareUpdater()"
    - DeviceScreen.kt DeviceViewModel constructor includes "catalog: FirmwareCatalog = TeFirmwareCatalog()"
    - DeviceScreen.kt imports android.util.Log
    - DeviceViewModelFirmwareTest.kt exists with at least 5 @Test methods
    - gradle test exits 0 — all 5+ test cases pass
    - Test case 3/4 (null/unparseable firmware) passes without catalog.latestVersion() being called
  </acceptance_criteria>
  <done>FirmwareUpdateState sealed type exists; DeviceViewModel emits Checking → UpdateAvailable/UpToDate/Unknown correctly; onOpenFirmwareUpdater stub is ready; all ViewModel firmware tests pass without network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → teenage.engineering | HTTPS GET for latest version; no user data in the request |
| DeviceViewModel → FirmwareUpdateState | Version strings are parsed through FirmwareVersion.parse() before any comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-04 | Spoofing | catalog.latestVersion() return value | mitigate | FirmwareVersion.parse() rejects non-numeric; major>50 guard in parseVersions; result validated before comparison |
| T-999.3-05 | Denial of Service | checkFirmwareUpdate() called repeatedly | mitigate | loadStats() has _statsLoading guard; checkFirmwareUpdate() is only called from loadStats(); LaunchedEffect(deviceState.connected) dedups trigger |
| T-999.3-SC | Tampering | androidx.browser:browser new dependency | mitigate | Well-known Google/AndroidX library; no slopcheck concern — version pinned to 1.8.0 |
</threat_model>

<verification>
Run all firmware tests together:
cd AndroidApp && ./gradlew :app:testDebugUnitTest --tests "com.ep133.sampletool.FirmwareVersionTest" --tests "com.ep133.sampletool.TeFirmwareCatalogParserTest" --tests "com.ep133.sampletool.DeviceViewModelFirmwareTest"

All must pass. BUILD SUCCESSFUL.

Also verify manifest and gradle:
grep "INTERNET" AndroidApp/app/src/main/AndroidManifest.xml
grep "androidx.browser" AndroidApp/app/build.gradle.kts
</verification>

<success_criteria>
- FirmwareUpdateState.UpdateAvailable is emitted when current version < latest
- FirmwareUpdateState.Unknown is emitted for null/unparseable firmware (no catalog call)
- FirmwareUpdateState.Unknown is emitted when catalog returns null (network failure)
- INTERNET permission in manifest; androidx.browser in build.gradle.kts
- DeviceViewModelFirmwareTest: all test cases pass
</success_criteria>

<output>
Create `.planning/phases/999.3-firmware-update-detection/999.3-firmware-update-detection-02-SUMMARY.md` when done.
</output>
