import SwiftUI

/// Placeholder for the Pads screen (port of `AndroidApp/.../ui/pads/PadsScreen.kt` pending).
/// The shell references this by name; the real screen replaces this file's contents in place.
struct PadsScreen: View {
    var body: some View {
        EP133ComingSoonPanel(section: "PADS")
    }
}
