import SwiftUI

/// Placeholder for the SAMPLES screen — loop chopper + kit builder host (port of
/// `AndroidApp/.../ui/kit/KitScreen.kt` pending). The shell references this by name; the real
/// screen replaces this file's contents in place.
struct KitScreen: View {
    var body: some View {
        EP133ComingSoonPanel(section: "SAMPLES")
    }
}
