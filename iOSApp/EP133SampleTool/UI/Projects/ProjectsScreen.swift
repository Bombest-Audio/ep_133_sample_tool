import SwiftUI

/// Placeholder for the Projects screen (port of `AndroidApp/.../ui/projects/ProjectsScreen.kt`
/// pending). The shell references this by name; the real screen replaces this file's contents
/// in place.
struct ProjectsScreen: View {
    var body: some View {
        EP133ComingSoonPanel(section: "PROJECTS")
    }
}
