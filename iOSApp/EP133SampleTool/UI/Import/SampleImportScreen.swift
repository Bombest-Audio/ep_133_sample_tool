import SwiftUI

/// Placeholder for the Sample Import screen (port of
/// `AndroidApp/.../ui/import/SampleImportScreen.kt` pending). The real screen replaces this
/// file's contents in place.
struct SampleImportScreen: View {
    var body: some View {
        EP133ComingSoonPanel(section: "IMPORT")
    }
}
