import SwiftUI

/// Placeholder for the Kit Builder pane — embedded in the SAMPLES screen's KIT mode (port of
/// `AndroidApp/.../ui/kitbuilder/KitBuilderScreen.kt` pending). The real screen replaces this
/// file's contents in place.
struct KitBuilderScreen: View {
    var body: some View {
        EP133ComingSoonPanel(section: "KIT BUILDER")
    }
}
